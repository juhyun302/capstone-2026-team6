# AGENTS.md

> 강의 4 산출물 — 도메인 용어집, 절대 규칙, 완료의 정의
> 아직 작성 전. 강의 4 작성 지침이 배포되면 채운다.

이 파일은 AI 에이전트가 **매 작업마다 함께 읽는 최소 어휘**다.
용어집의 대표어는 `docs/ontology.yaml`의 클래스명과 일치해야 한다.

> ⚠️ 작성 후 `CLAUDE.md`에서 `@AGENTS.md`로 import 해야 실제로 읽힌다.
> import 하지 않으면 파일만 있고 읽히지 않는다. `/context`로 확인할 것.

## 용어집

<!-- docs/ontology.yaml 의 대표어를 여기로 옮긴다 -->

## 절대 규칙

<!-- 사람이 정한다. AI가 정하면 그 기준으로 AI를 검증할 수 없다. -->

## 완료의 정의

<!-- docs/SPEC.md 의 수용 기준(AC)과 연결된다 -->
